/*
 constraints -- set of rules that we follow during the creation of the tables
	* primary key --> UNIQUE values and NOT NULL
    * NOT NULL --> enforces data in field
    * UNIQUE --> Unique values and allows NUll
    * CHECK --> validates data
    * DEFAULT --> Sets Default values
    * FOREIGN KEY --> Allows duplicate values and help in creating relationship
*/




